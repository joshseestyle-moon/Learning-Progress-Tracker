# 交接：2026-07-13 專案狀態總覽

> 本檔用途：接手 CLAUDE.md 外移的版本史與現況細節。CLAUDE.md 只留不變量與路由，細節看這裡。

## 1. 現況一覽（本輪成果）

| 項目 | 狀態 | 位置 |
|---|---|---|
| web 主系統 | v3.9＋最佳化輪＋考科過濾，全部完成驗證 | main `895cbb9`（**領先 origin 2 commit 未 push**） |
| 程式碼最佳化輪（2026-07-11） | 完成：2 個日期 bug、區間頁快取重構（切 chip 0/2 fetch）、9 頁世代守衛、RARITY_PTS 收斂、better-sqlite3 12.11.1 | 已合 main；稽核與計畫在 `docs/audit-2026-07-11-*.md`、`docs/plan-optimize-2026-07-11.md`（含執行紀錄） |
| 五頁科目選單只列考科 | 完成（exams/homework/studylog/grades/chapters 各一行過濾；資料端 One Study ×2 已改標考科，備份 app.db.bak-2026-07-12） | main `895cbb9` |
| **原生 Android App（獨立案子）** | **P0-P6 全部完成並逐階段 fresh 驗收**：資料層＋匯入、六唯讀頁、遊戲化引擎、徽章/獎勵店、分析/區間/行事曆、報告 PDF/備份/設定、CRUD 補齊＋考科過濾 | 分支 `dev-android-app`（10 commits，**未合 main**）；規劃在 `app/00-06.md`＋`90-source-map.md`；APK：`app/android/app/build/outputs/apk/debug/` |
| copy-user-data.js 升級 M25 | 完成並驗收（含 --allow-any 護欄）；已對正式庫執行過（測試用帳號B = 邦正複本） | 分支 `chore-copy-script-m25`（**未合 main**，建議合） |

## 2. 版本史（自 CLAUDE.md 外移，細節看各 plan 檔）

- **v3.9**（`docs/plan-period-scope.md`）：四明細頁依學習區間過濾；共用 `public/js/period-filter.js`（現含 `initPeriodFilter`/`periodLabel`/`localD`/`inRange`/`mountPeriodScoped`，chips 存 localStorage `periodScope`）；homework「全部」=滾動±30天；chapters 推導式歸屬。後續：4 個正確性 bug 修復（世代守衛 `_gen` 模式）、schema.sql 同步 M25（db.exec(schema) 每次啟動都跑，語句必須 IF NOT EXISTS）、成績頁區間過濾、目標表單類型說明。產品決策：作業清單與目標不連動。
- **飛輪計畫**（`docs/plan-flywheel.md`）：五階段（XP/combo/驚喜 Mig22-23、補救引擎 Mig24、成長頁）完成；F1-F10 code-review 全修（`docs/review-findings-2026-07-10.md`）。**已合入 main**。
- **v3.8**（`docs/plan-calendar-tasks.md`）：assignments 正名「事件」（純 i18n）；事件完成不給 XP/驚喜（徽章檢查保留）；daily_tasks 單向上行事曆（subject 可 null 有色彩 fallback）。
- **v3.7**（`docs/plan-report.md`）：每 7 天自動備份 `data/backups/`（保留 8 份）；報告 API＋學習歷程報告頁（A4 列印）；儀表板週回顧卡。

## 3. 遊戲化系統慣例（跨 session 常查）

- XP reason：`study:<id>`、`chapter:<progressId>`、`task:<taskId>:<partNum|done>`、`goal:<id>`、`quest:<id>`；防重靠 grantOnce（查 xp_log reason）；週/區間統計一律排除 `backfill:%`。
- point_log reason：`exchange:custom_<id>`／`exchange:<id>`／`redeem:<id>`／`surprise:<date>`／`quest:<id>`／`badge:<id>`（舊資料）；顯示端 CASE 在 routes/shop.js:60-103＋前端補丁。
- **徽章兌換循環（2026-07-13 查證，勿誤診為漏頒）**：兌換刪 user_badges 列 → 下次活動 checkBadges 自動再頒發 → 可再兌換；同枚每日限一次（checker.js 的 exchangedTodaySet）。孩子每天在做這個循環，user_badges 空≠漏頒。
- 前端 toast 通道：`gamify-result`（api.js 自動派發）與 `app-toast`（手動），監聽在 app.html。
- 測試資料清理：測完刪測試用帳號的 goals/periods/catchup_quests 與 xp_log/daily_reward_log/point_log 測試列；用過 switchLang 把 users.lang 改回 zh-TW。

## 4. Android App 快速事實（細節見 `app/00-overview.md` 與 memory）

Kotlin+Compose+Room、零權限（資料不出裝置）、compileSdk 36（計畫 35 因 SDK 未裝改用，AGP 8.11.2/Gradle 8.13）、匯入=逐表複製（copy-user-data 模式）。驗證基礎設施：AVD `test36`（headless）＋黃金對帳測試（獨立 SQL vs DAO，三帳號）。已知限制：無 FK cascade（帳號刪除未做）、「清除全部資料」/匯入未破壞性實測。模擬器內是測試髒資料。

## 5. 未決事項（等使用者決定）

1. main 領先 origin 2 commit（Android 規劃文件＋考科過濾）——要 push 說一聲。
2. `dev-android-app` 合併時機——建議實機用幾天再合。
3. `chore-copy-script-m25` 建議合入 main（工具已在用）。
4. 已合併完成的舊分支（dev-2026-07-08/dev-2026-07-11/fix-v39-bugs/feat-grades-period-scope/docs-2026-07-11/chore-2026-07-11-schema-sync/chore-optimize-2026-07-11/plan-android-app/feat-exam-subjects-filter）可刪可留，等指示。
5. web 效能 backlog（audit-2026-07-11-optimize.md 判定暫不做）：checker.js 事件分流、autoBackup 延後啟動、toast/進度條收斂。
6. 正式 DB 備份還原點：`data/app.db.bak-2026-07-11`（複製前）、`bak-2026-07-12`（One Study 改標前）、`bak-2026-07-13`（未使用，當日快照）。

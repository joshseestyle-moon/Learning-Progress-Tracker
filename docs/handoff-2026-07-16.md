# 交接：2026-07-16 專案狀態總覽

> 本檔用途：取代 `handoff-2026-07-13.md` 作為現況細節與版本史入口。CLAUDE.md 只留不變量與路由，細節看這裡。

## 1. 現況一覽（本輪成果）

| 項目 | 狀態 | 位置 |
|---|---|---|
| web 主系統 | v3.9＋最佳化輪＋考科過濾＋文案/流程優化（獎勵透明化、目標已達成確認）＋copy-user-data M25 升級 | main `30f2d7c`（**領先 origin 3 commits：copy-user-data M25 的 2 個原始 commit＋merge commit，未 push**） |
| 使用手冊＋flow-review | 完成：`docs/userguide.md`、`docs/flow-review-2026-07-14.md` | 已合 main |
| 獎勵透明化文案 A1-A4 | 完成 | 已合 main |
| 目標建立「已達成」偵測先確認（flow-review B5 方案 b） | 完成 | 已合 main |
| copy-user-data.js M25 升級 | 完成並驗收（含 `--allow-any` 護欄）；已對正式庫執行過（測試用帳號B = 邦正複本） | **本次已合 main**（`merge: chore-copy-script-m25 into main`），`npm test` 69/69 綠 |
| 舊分支清理 | **本次已處理**：13 個已完全合併的分支（含本次合併的 chore-copy-script-m25）已用 `git branch -d`（安全刪除，非合併狀態會拒絕）全數刪除 | 目前只剩 `main`／`dev-android-app` |
| **原生 Android App（獨立案子）** | P0-P6 全部完成並逐階段 fresh 驗收，狀態未變 | 分支 `dev-android-app`（未合 main，未變動） |

## 2. 版本史（自 handoff-2026-07-13 之後新增）

- **獎勵透明化 A1-A4**：文案調整，讓孩子清楚知道獎勵/點數怎麼來，符合「養自發性」原則（見 memory `feedback_reward_framing`）。
- **目標建立時偵測「已達成」（flow-review B5 方案 b）**：新增目標時若條件其實已達標，先跳確認而非直接靜默建立。
- **使用手冊＋flow-review-2026-07-14**：給孩子的操作手冊文件化，並做一輪端到端流程檢視。
- **copy-user-data.js 升級 Migration 19-25**：涵蓋近期 schema 新增的表格，`--allow-any` 護欄防止誤複製到非測試帳號；規格見 `docs/spec-copy-user-data-m25.md`。

（07-13 之前的版本史：v3.9 period-scope、飛輪計畫、v3.8 calendar-tasks、v3.7 report，見 `docs/handoff-2026-07-13.md` §2，內容未變不重複列出。）

## 3. 遊戲化系統慣例（跨 session 常查，沿用自 07-13，未變動）

- XP reason：`study:<id>`、`chapter:<progressId>`、`task:<taskId>:<partNum|done>`、`goal:<id>`、`quest:<id>`；防重靠 grantOnce（查 xp_log reason）；週/區間統計一律排除 `backfill:%`。
- point_log reason：`exchange:custom_<id>`／`exchange:<id>`／`redeem:<id>`／`surprise:<date>`／`quest:<id>`／`badge:<id>`（舊資料）；顯示端 CASE 在 routes/shop.js:60-103＋前端補丁。
- **徽章兌換循環（勿誤診為漏頒）**：兌換刪 user_badges 列 → 下次活動 checkBadges 自動再頒發 → 可再兌換；同枚每日限一次（checker.js 的 exchangedTodaySet）。user_badges 空列 ≠ 漏頒。
- 前端 toast 通道：`gamify-result`（api.js 自動派發）與 `app-toast`（手動），監聽在 app.html。
- 測試資料清理：測完刪測試用帳號的 goals/periods/catchup_quests 與 xp_log/daily_reward_log/point_log 測試列；用過 switchLang 把 users.lang 改回 zh-TW。

## 4. Android App 快速事實（沿用自 07-13，未變動）

Kotlin+Compose+Room、零權限（資料不出裝置）、compileSdk 36、AGP 8.11.2/Gradle 8.13、匯入=逐表複製（copy-user-data 模式）。驗證基礎設施：AVD `test36`（headless）＋黃金對帳測試。已知限制：無 FK cascade（帳號刪除未做）、「清除全部資料」/匯入未破壞性實測。

## 5. 未決事項（等使用者決定）

1. **main 領先 origin 3 commits**（copy-user-data M25 的 2 個原始 commit＋本次 merge commit）——要 push 說一聲。
2. **`dev-android-app` 合併時機**——建議實機用幾天再合，狀態未變。
3. ~~`chore-copy-script-m25` 建議合入 main~~ **已處理**（本次合併，測試 69/69 綠）。
4. ~~舊分支清理~~ **已處理**（13 個已完全合併分支已刪除，含 3 個 07-13 之後新增的：`chore-docs-2026-07-13`／`dev-goal-confirm-2026-07-14`／`dev-ux-copy-2026-07-14`）。

5. **web 效能 backlog**（來源：`docs/audit-2026-07-11-optimize.md`，2026-07-11 判定「現場仍成立」但暫不做，供決策參考）：

   | # | 項目 | 效益 | 風險 | 備註 |
   |---|---|---|---|---|
   | #6 | chapters/exams 頁切區間 chip 都重抓整批 API，其實只需本地重新過濾 | 高（切區間從 2 次 API 降到 0 次） | 低 | `chapters.js:34-39`／`exams.js:25-27` |
   | #7 | studylog 頁切 chip 重抓全部 6 個端點，其中 4 個與 scope 無關 | 高（尤其省 364 天 heatmap 查詢） | 低 | `studylog.js:30-43` |
   | #8 | 4 頁（chapters/exams/homework/studylog）各自手植 `_el`/`_scope`/`_gen`/render 樣板，重複度因 `_gen` 世代守衛修復又增加 | 非效能，降低未來漏改風險 | 中（觸及 4 頁載入路徑，需逐頁驗證） | 建議與 #6/#7 合併一個 PR 一次做完 |
   | #9 | inRange 日期落點判斷手刻三處 | 非效能，一致性 | 低 | 順手做 |
   | （自查）goals N+1 | `routes/goals.js`＋`utils/goalMetrics.js` 每個 goal 一條 SQL 且未快取 prepared statement | 低（goal 數量通常個位數~十位數），但 statement 快取是穩賺不賠 | 低 | `metricsFor` 內 `db.prepare` 應搬到模組頂層 |
   | （自查）checker.js 全量掃描 | 每次活動觸發都跑全部 ~18 條徽章判定查詢，不分事件類型 | 中（實測延遲可能不明顯，資料量小） | **中～高**（遊戲化核心邏輯，需覆蓋每種 event.type × 每類徽章才安全重構） | 建議「已知可優化但暫不建議動」，除非未來使用者數增加 |
   | 依賴 | `better-sqlite3` 12.10.0 → 12.11.1（minor） | 安全更新 | 低，但 native module 重新編譯有失敗風險 | 建議走小分支＋npm test＋重啟兩次驗證 |

   **建議優先序**（若要做）：#6+#7+#8+#9 合併一個重構 PR（同源、可一次驗證）＞ goals prepared statement 快取（零風險小改）＞ better-sqlite3 升級 ＞ checker.js 分流（風險最高，量測前不建議）。

6. **正式 DB 備份還原點**（`data/` 目錄現況，供判斷是否可清理）：

   | 檔案 | 日期 | 大小 |
   |---|---|---|
   | `app.db.bak-2026-07-10`（+shm/wal） | 07-10 | 397KB |
   | `app.db.bak-2026-07-11` | 07-11 | 418KB |
   | `app.db.bak-2026-07-12` | 07-12 | 528KB |
   | `app.db.bak-2026-07-13` | 07-13 | 528KB |
   | `app.db.bak-2026-07-14` | 07-14 | 545KB |
   | `data/backups/auto-2026-07-10.db` | 07-10（自動備份，v3.7 機制，保留上限 8 份，目前只有 1 份） | 384KB |
   | `app.db`（現行） | 07-15/16 | 569KB |

   全部是修改前的手動快照（依 CLAUDE.md 不變量 3：動 migration 前必備份），加上一份自動備份。都不是敏感度以外的問題，只是單純「留幾份」的取捨——目前 5 份手動 + 1 份自動，佔用約 2.4MB，體積很小，保留成本低；若要清理，建議只留最近 1-2 份手動快照（更早的已被後續版本覆蓋、還原意義不大）。**未執行任何刪除，等你決定。**

## 6. 驗收記錄（本次操作）

- `npm test`：69/69 綠（merge 前後測試套件通過，確認 copy-user-data.js 合併未破壞其他模組）。
- 分支刪除全部用 `git branch -d`（非 `-D`）：Git 會在分支未完全合併時拒絕刪除，13 個分支均刪除成功即代表確實已完全合併，無資料遺失風險。
- 未執行 `git push`：main 領先 origin 3 commits，等使用者確認（見 §5.1）。

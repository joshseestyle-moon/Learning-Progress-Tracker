# 交接與續辦清單 — 2026-07-11（v3.9 收尾 session）

> 狀態：本檔原為 token 額度中斷時的續辦清單；同一 session 內額度恢復後，第二節（code-review）與第三節（schema.sql 技術債）皆已完成，結果記在各節末尾。保留本檔作歷史記錄，供之後追查這兩項修復的理由與驗證過程。

## 〇、後續完成摘要（同一 session 內補記）

- **v3.9 明細頁 code-review**：8 角度 finder + 9 項 1-vote 驗證，**全部 9 個候選都 CONFIRMED**。已用 ReportFindings 回報，未自動修復（審查結果，交給使用者決定是否要修）。詳見第二節。
- **schema.sql 技術債**：已修復並驗證，commit 於分支 `chore-2026-07-11-schema-sync`。**過程中發現原本的風險評估是錯的**——`db.exec(schema)` 其實每次啟動都會對正式 DB 執行（不是只有首次建庫），只是舊 schema.sql 因為停在 M9 之前的舊表、且用 IF NOT EXISTS 而顯得無害；修復時額外發現至少 5 個 migration 內建表語句（point_log/reward_items/redemption_log/xp_log/badge_exchange_log）本身沒有 IF NOT EXISTS，只靠外層 guard 冪等，若照抄會讓新 schema.sql 在既有 DB 上直接噴錯、炸掉每次重啟。已改用「拋棄式 DB 產生真實 schema、逐條強制補 IF NOT EXISTS、三種情境測試（全新建庫/schema.sql 單獨建庫比對欄位一致/對已遷移真實資料複本套用確認 24 表列數不變）+ 正式伺服器兩次重啟」的流程處理，詳見第三節。

## 一、已完成並已上線（無須再動）

- **v3.9 明細頁區間過濾**：Phase A/B/C 全部完成、驗證、合併回 `main` 並 push。
  - Phase A `3502ae5`（exams+homework+growth 接上共用 `period-filter.js`）
  - Phase B `e3a493f`（studylog 列表＋學習總覽依區間，後端 `/summary` 加可選 from/to）
  - Phase C `033e105`（chapters 推導式歸屬：`created_at` 或任一 review `scheduled_date` 落在區間內）
  - `540641e` CLAUDE.md 標記 v3.9
- **參考文件更新到 v3.9**：`85cc704`（README/SYSTEM_DOC/TECH_SPEC，已合併 main 並 push）。`PROJECT_MEMORY.md` 也已更新但屬 .gitignore（本機用，不進 repo）。
- **遠端狀態**：`origin/main` 已到 `85cc704`。分支 `dev-2026-07-11`、`docs-2026-07-11` 皆已 fast-forward 併入 main。
- **測試帳號（user7）資料**：依使用者指示「保留現狀」——保留 periods 20（114 下學期 2026-02-01~06-30）、21（114 暑假 2026-07-01~08-31）與 Phase A 的考試/作業當長期測試素材；只清掉本 session 驗證用的暫時資料（study_log 162＋其 xp/point/reward log、ch76 的 2026-07-15 review、拋棄式 period 22）。user7 lang 仍 zh-TW。

## 二、續辦 1：v3.9 的 code-review — 已完成

**已於同一 session 完成**：8 角度 finder + 逐項 1-vote 驗證，9 個候選全部 CONFIRMED。以下發現**均為審查回報、尚未修復**，是給使用者決定要不要修的清單（v3.9 已在 main 上線）。

### 正確性 bug（4）
| # | 檔案:行 | 問題 | 建議修法 |
|---|---|---|---|
| 1 | chapters.js:41 / exams.js:30 / homework.js:22 / studylog.js:47 | 切頁離開時若非同步 `refresh()` 尚未完成，`router.js:59` 已把 `#view` 內容換掉，`_el.querySelector('#X-body')` 回 null，`body.innerHTML=` 拋 TypeError（未處理 rejection）。此 diff 把「靜默寫錯頁」變成「硬崩潰」 | refresh() 前 null-guard body；或加 generation/AbortController，切頁即作廢舊 refresh |
| 2 | homework.js:18 / studylog.js:33 | 快速切換區間 chip 時，`range`/`q` 在 `await` 前由 `_scope` 算好，慢回應可蓋掉新選取的正確畫面（chip 高亮與內容不符）。chapters/exams 因 await 後才用即時 `_scope` 過濾而自我修復、不受影響 | 加 sequence token 或 await 後重新比對 `_scope`，不符就丟棄 |
| 3 | homework.js:186 | 選了不含今天的區間後新增作業（日期框預設今天、無範圍限制），POST 成功但 `refresh()` 依區間範圍抓、後端過濾掉 → 作業從畫面消失、無提示 | 新增後若日期落在區間外則提示；或新增後抓全量；或限制日期框範圍 |
| 4 | homework.js:183 | 每次新增作業都失焦：`titleInput.focus()` 後 `refresh()` 重建整個 DOM（含 `#hw-title`），新節點無人重新聚焦，連續輸入體驗被破壞 | refresh 後重新 focus，或還原今天日期的樂觀更新路徑 |

### API 一致性缺口（1）
| # | 檔案:行 | 問題 | 建議修法 |
|---|---|---|---|
| 5 | routes/studylog.js:80 | `GET /summary` 只在 `from && to` 都給時才過濾，只給其一則靜默回全期總計、無錯誤——與同專案 `report.js`、`daily-tasks.js` 的 `DATE_RE` 400 驗證不一致 | 補「兩者皆給或皆不給」的 DATE_RE 驗證，部分區間回 400 |

### 效能／重複（4，非 bug，品質改善）
| # | 檔案:行 | 問題 | 建議修法 |
|---|---|---|---|
| 6 | chapters.js:33 / exams.js:24 | 每次點 chip 都重抓完整資料集，但區間只影響客戶端過濾 → 每次點擊 2 個多餘網路往返 | 快取已抓資料，切 scope 只重跑過濾 |
| 7 | studylog.js:34 | 每次點 chip 重抓全部 6 個端點（含 364 天熱力圖），但自身註解已說只有 2 個依 scope 變動 | 切 scope 只重抓 `/studylog` 與 `/summary` |
| 8 | chapters/exams/homework/studylog | 四頁重複 `_el`/`_scope` + `initPeriodFilter` 樣板，未來修 #1/#2 要改四處 | 讓 period-filter.js 擁有 `_el`/`_scope` 與掛載，各頁只提供 refresh(scope) |
| 9 | chapters.js:29 / exams.js:28 | 日期落在區間內的比對 `d>=from && d<=to` 手寫三處（注意：`exam_date`/`scheduled_date` 是純日期欄不需 localD，只有 `created_at` 需要） | period-filter.js 匯出 `inRange(date, scope)` 共用 |

> 修復順序建議：#1 與 #2 同源（都因 module-level `_el`/`_scope` 缺 race 防護），可與 #8 一起重構解決；#3、#4、#5 為獨立小修。**動手修前請先向使用者確認要修哪些、以及開哪個分支。**

<details><summary>原始續辦紀錄（已完成，保留供追溯）</summary>

狀態：使用者要求跑「聚焦版 /code-review」，我已備好八個 finder 角度的派工，但使用者中斷了（拒絕了 7 個 agent 呼叫）。**尚未產出任何 finding。**

- **審查範圍檔案**：`public/js/period-filter.js`（新共用元件）、`public/js/chapters.js`、`public/js/exams.js`、`public/js/homework.js`、`public/js/studylog.js`、`routes/studylog.js`。
- **重建 diff**（原本放 $CLAUDE_JOB_DIR/tmp，job 清掉就沒了，用這兩段重建）：
  ```
  git diff 3502ae5~1..033e105 -- public/js/period-filter.js public/js/chapters.js routes/studylog.js public/js/exams.js public/js/homework.js
  git diff e3a493f~1..e3a493f -- public/js/studylog.js
  ```
- **審查重點**（使用者指定）：邏輯正確性、邊界情況（無區間帳號、localStorage 存了已刪除的 period id、空 scope）、新查詢字串組裝的 XSS/注入疑慮。
- **八個角度**：line-by-line 掃描、removed-behavior 稽核、cross-file tracer、reuse、simplification、efficiency、altitude、conventions(CLAUDE.md)。每角度 ≤6 候選 → 1-vote 驗證（recall-biased）→ ≤10 findings，最嚴重在前。
- **已知可先檢視的點**（我掃過但未定案，供 finder 參考，勿當結論）：
  1. chapters `inScope()` 用推導式客戶端過濾，exams 用客戶端 `exam_date` 過濾，homework「全部」用 ±30 天滾動窗、區間模式用後端範圍——三頁三種做法，altitude 角度可能會提「該不該收斂成單一 period→date-range 原語」。
  2. `initPeriodFilter` 在 `await get('/periods')` 後同步呼叫 `onChange(current)`；各頁 render() 是否有 DOM/狀態就緒的競態。
  3. 四頁共用單一 localStorage key `periodScope`——切換一頁的區間會靜默影響下一頁，屬設計取捨，需確認是否為預期。
  4. `routes/studylog.js` `/summary` 與前端送的 from/to 需「兩者皆給才篩選」，前端 `?from=..&to=..` 一定成對，OK；`GET /studylog?from&to`（list 端點）則是各自獨立綁定，也 OK（已用 better-sqlite3 參數綁定，無注入）。
- **產出**：把 findings（若有）回報給使用者決定要不要修；不要未經同意就改 code。

</details>

## 三、續辦 2（技術債）：`db/schema.sql` 與 db.js migrations 脫節 — 已完成

> **已於同一 session 完成並驗證**（commit `d775d99`，分支 `chore-2026-07-11-schema-sync`）。原始分析與修法記錄如下；實際修復重點與「原風險評估錯誤」的更正見本檔第〇節。

- **現況**：`db/schema.sql` 只停在最初版（users/subjects/timetable_slots/assignments/exams/chapters/chapter_progress/study_log/grades）。**Migration 9 之後所有新表與加欄只存在於 `db/db.js` 的 `openAndMigrate()` 內**，schema.sql 從未同步。
- **影響**：schema.sql 已不是可信的 schema 真相來源（新 DB 靠 db.js migration 建，不靠 schema.sql 建完整結構）；但因為建庫流程走 db.js，功能不受影響——純屬文件/可讀性技術債。文件（SYSTEM_DOC/TECH_SPEC）已據實標註「權威 schema 以 db.js migrations 為準」。
- **缺的表（M9→M25）**：user_badges、point_log、reward_items、redemption_log、custom_badges、custom_badge_earned、badge_exchange_log、daily_tasks、daily_task_parts、periods、goals、xp_log、daily_reward_log、catchup_quests、catchup_quest_items；加欄：users(lang, daily/weekly_goal_minutes)、subjects(category)、chapter_progress(original_scheduled_date)。
- **建議修法（擇一，動手前先問使用者要不要做、以及是否要在新分支）**：
  - (A) 從一個「跑完所有 migration 的乾淨 DB」dump schema：`DB_PATH=<temp> node -e "require('./db/db')"` 建庫後，用 better-sqlite3 讀 `sqlite_master` 或 sqlite3 `.schema` 匯出 CREATE 語句，整理進 schema.sql（保留註解與索引）。**注意：require db.js 有副作用會跑 migration，務必先設 `DB_PATH` 指向拋棄式檔，別碰正式 `data/app.db`。**
  - (B) 手動照 db.js migration 內容把新表 CREATE 補進 schema.sql。
  - 兩者皆須：改完用「重啟伺服器兩次皆無錯」驗證（不變量）；schema.sql 僅供參考/新庫初始化，改它不影響既有 DB。
- **不變量提醒**：動 DB/migration 前備份 `data/app.db`；此技術債修復本身不新增 migration、不動既有資料，但若順手改到 db.js 就要走 migration 冪等規範。

## 四、如何續辦（排程 fire 時的第一步）

1. 讀本檔 + 全域/專案 CLAUDE.md 不變量。
2. 確認在 `main`、工作區乾淨（`git status`）；若要動 code 先開新分支（勿直接動 main）。
3. 先做「續辦 1」code-review（唯讀、安全）→ 回報 findings。
4. 再做「續辦 2」schema.sql（會改檔）→ 動手前先向使用者確認範圍與分支。

# Default: auto line ending conversion
* text=auto

# Shell scripts must always use LF (otherwise Mac/Linux cannot execute them)
*.sh text eol=lf

# Windows batch files use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
